function useCharacterPortraitUrl(_characterId: number) {
  const isLoading = false;
  const portraitUrl = null;

  return {
    isLoading,
    portraitUrl
  };
}

export default useCharacterPortraitUrl;
